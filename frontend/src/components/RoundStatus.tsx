import type { Player, PublicRound } from '../types/game'
export function RoundStatus({round,players}:{round:PublicRound;players:Player[]}){return <aside className="round-status panel"><p className="eyebrow" data-testid="round-number">Runda {round.number}</p><h2>{round.phase==='PLANNING'?'Planering':round.phase==='RESOLVING'?'Beräknar rörelser':'Uppspelning'}</h2><p data-testid="game-phase">Fas {round.phase}</p><ul>{players.map(p=><li key={p.id}><span>{p.name}</span><strong>{round.ready[p.id]?'Redo':'Väljer…'}</strong></li>)}</ul></aside>}
